services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./checkpoints:/app/checkpoints
      - ./data:/app/data
    restart: unless-stopped
